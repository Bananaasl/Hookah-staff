CREATE TABLE IF NOT EXISTS brands_and_tastes
(
    id       BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    brand_id BIGINT,
    taste_id BIGINT,
    CONSTRAINT pk_brandsandtastes PRIMARY KEY (id)
);

ALTER TABLE brands_and_tastes
    ADD CONSTRAINT FK_BRANDSANDTASTES_ON_BRAND FOREIGN KEY (brand_id) REFERENCES brand (id);

ALTER TABLE brands_and_tastes
    ADD CONSTRAINT FK_BRANDSANDTASTES_ON_TASTE FOREIGN KEY (taste_id) REFERENCES taste (id);