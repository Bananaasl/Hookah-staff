# Установка OCR сервиса

## Python скрипт с Tesseract OCR (Текущее решение)

Решение использует Python скрипт с Tesseract OCR для распознавания текста с фотографий.
**Преимущества:**
- ✅ Нет ограничений по размеру файла (поддерживает большие фото с iPhone)
- ✅ Полностью бесплатно
- ✅ Работает локально, не требует интернета
- ✅ Поддержка русского и английского языков

### Шаг 1: Установка Tesseract OCR

#### macOS:
```bash
brew install tesseract
brew install tesseract-lang  # Для поддержки русского языка
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt-get update
sudo apt-get install tesseract-ocr
sudo apt-get install tesseract-ocr-rus  # Для поддержки русского языка
```

#### Windows:
1. Скачайте установщик с https://github.com/UB-Mannheim/tesseract/wiki
2. Установите Tesseract
3. Добавьте путь к Tesseract в PATH (обычно `C:\Program Files\Tesseract-OCR`)
4. Установите языковые пакеты (rus, eng)

### Шаг 2: Установка Python зависимостей

```bash
# Убедитесь, что у вас установлен Python 3.7+
python3 --version

# Установите зависимости
pip3 install -r requirements.txt
```

### Шаг 3: Настройка приложения

В файле `application.properties` уже настроены параметры по умолчанию:
```properties
ocr.python.script.path=ocr_service.py
ocr.python.command=python3
```

Если Python скрипт находится в другом месте или используется другая команда Python, измените эти параметры.

### Шаг 4: Проверка установки

Проверьте, что Tesseract установлен:
```bash
tesseract --version
```

Проверьте, что Python скрипт работает:
```bash
python3 ocr_service.py
# Должен вывести ошибку о том, что ожидается JSON (это нормально)
```

## Использование

После установки функционал OCR будет работать автоматически:

1. Откройте приложение
2. Нажмите "📷 Загрузить фото"
3. Загрузите фото накладной с ценами
4. Загрузите одно или несколько фото пачек табака
5. Нажмите "🔍 Распознать табаки"
6. Проверьте результаты и нажмите "✅ Добавить в привоз"

## Решение проблем

### Ошибка "Tesseract not found"
- Убедитесь, что Tesseract установлен и доступен в PATH
- Для Windows: проверьте, что путь к Tesseract добавлен в системные переменные

### Ошибка "No module named 'pytesseract'"
- Установите зависимости: `pip3 install -r requirements.txt`

### Плохое качество распознавания
- Убедитесь, что фото хорошего качества и хорошо освещено
- Текст должен быть четким и контрастным
- Попробуйте сделать фото при лучшем освещении

### Python скрипт не запускается
- Проверьте, что используется правильная команда Python (`python3` или `python`)
- Убедитесь, что скрипт `ocr_service.py` находится в корне проекта
- Проверьте права доступа к файлу
